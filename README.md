# Wholphin Intent Tester

App Android TV minimale per verificare il playback via Android Intent.

Intent principale: `com.github.damontecres.wholphin.PLAYBACK` con extra `itemId`.
Fallback: `wholphin://play?itemId=...`.

Non contiene API key, password o credenziali Jellyfin.
